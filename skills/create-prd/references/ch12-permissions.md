# 第12章 角色和权限 — 生成指引

## 章节目标

以权限矩阵的形式，精确定义各角色在各页面/功能上的操作权限，含数据权限设计。

> 💡 方法论基础：
> - RBAC 模型（用户→角色→权限集→菜单/页面/页面元素）
> - 三层权限控制：菜单级 → 页面级 → 功能元素级
> - 数据权限：基于组织架构树的数据范围控制

## 生成结构 — 业务型/交易型产品（完整 RBAC）

```md
## 12、角色和权限

### 12.1 角色定义
| 角色名称 | 角色说明 | 典型人群 | 数据范围 |
| --- | --- | --- | --- |

### 12.2 功能权限矩阵
| 序号 | 一级导航 | 页面 | 页面元素 | {角色A} | {角色B} |
| --- | --- | --- | --- | --- | --- |

### 12.3 数据权限设计
| 角色 | 数据范围规则 | 说明 |
| --- | --- | --- |

### 12.4 管理功能
- **用户管理**：创建/编辑/停用/启用
- **角色管理**：创建/编辑/删除角色，配置权限集
```

## 生成结构 — 工具型产品（简化权限）

```md
### 12.1 角色定义
| 角色名称 | 角色说明 | 权限范围 |
| --- | --- | --- |
| 管理员 | 系统配置、用户管理 | 全部 |
| 普通用户 | 日常使用 | 除系统配置外 |

### 12.2 功能权限差异
| 功能 | 管理员 | 普通用户 |
| --- | --- | --- |
```

## 生成结构 — 基础服务型产品（API 权限）

```md
### 12.1 认证方式：API Key / OAuth 2.0 / JWT

### 12.2 API 权限控制
| API 分组 | 权限级别 | 限流规则 | 说明 |
| --- | --- | --- | --- |
```

## SaaS 多租户补充
- 租户隔离级别（DB/Schema/Table/Row）
- 租户管理员 vs 平台管理员的权限边界
- 跨租户数据隔离规则

## 质量标准
- 所有角色都有明确定义和说明
- 业务型产品的权限矩阵精确到页面元素级别
- 有数据权限设计（不只是功能权限）
- 管理功能（用户/角色管理）已覆盖
